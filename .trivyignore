# Trivy ignore list for known transitive CVEs we cannot fix in this repo.
# Each entry explains the constraint source and removal criteria. Delete
# entries as soon as the upstream pin is relaxed and the dependency is
# picked up through uv.lock.
#
# Currently empty: CVE-2026-26007 (cryptography, fixed in 46.0.5),
# CVE-2025-4565 and CVE-2026-0994 (protobuf, fixed in 5.29.5+ / 6.31.1+)
# were removed after verifying the locked versions (cryptography 50.0.1,
# protobuf 7.36.1 via override-dependencies) scan clean without them.

